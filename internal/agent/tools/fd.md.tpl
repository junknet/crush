Find files by path or name using the `fd` tool (or `rg --files` as fallback). Results are sorted by path length and respect `.gitignore`.

This tool is the ONLY filename-search tool. Do not call `find` via `bash` — use this `fd` tool instead.
