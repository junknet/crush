搜索工具，由 ripgrep 驱动。两种模式：

**模式一（默认）：搜索文件内容**
- `pattern` 为正则表达式，在所有文件内容里搜索匹配行
- 按文件修改时间排序，最多返回 100 条结果
- `include` 可限制文件类型，如 `*.go`、`*.{ts,tsx}`（不含路径前缀）
- `literal_text=true` 时将 pattern 作为纯文本（自动转义正则特殊字符）

**模式二（`files_only=true`）：按文件名/路径搜索**
- 等价于 `fd` / `find -name` 功能
- `pattern` 支持：
  - **glob**：`*.go`、`**/*.ts`、`src/*.nim`（推荐，自动转换为内部正则）
  - **正则**：`\.go$`、`impl_.*\.nim`
- 按路径长度排序（短路径优先）
- `include` 可进一步限制文件扩展名

**注意**：
- 这是代码库内唯一的内容/文件名搜索工具。禁止在 bash 里调用 `grep`、`rg`、`find` 做代码库搜索
- 结构化代码搜索（如"找所有调用了 X 函数的地方"）用 `ast_grep`
- 外部目录（~/Desktop 等）不受 .gitignore 过滤

